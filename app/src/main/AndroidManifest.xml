<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="club.biblelocker.biblelocker">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="com.samsung.android.providers.context.permission.WRITE_USE_APP_FEATURE_SURVEY" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BroadcastReceiver.BootReceiver"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".BroadcastReceiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Activities.MotionLock" />
        <activity android:name=".Activities.MotionLockCheck" />
        <activity android:name=".Activities.PatternLock" />
        <activity android:name=".Activities.PatternLockCheck" />
        <activity android:name=".Activities.PatternTest" />
        <activity android:name=".Activities.PinLock" />
        <activity android:name=".Activities.PinLockCheck" />
        <activity android:name=".Activities.AlarmSetting" />
        <activity android:name=".Activities.TestActivity" />
    </application>

</manifest>